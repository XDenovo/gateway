# Changesets

Add a Changeset whenever the public `@xdenovo/gateway-client` contract or package behavior
changes. Changesets maintain the reviewed Version PR; they are not a substitute for HTTP `/v1`
backward-compatibility review.
